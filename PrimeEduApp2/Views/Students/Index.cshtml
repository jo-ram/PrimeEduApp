@model IEnumerable<PrimeEduApp2.Models.Student>

@{
    ViewBag.Title = "Index";
}

@*<h2> @Html.DisplayNameFor(model => model.Classroom.ClassroomName)</h2>*@

<h2>List of all the students</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Classroom.ClassroomName)
        </th>
        <th>
           First Name
        </th>
        <th>
            Last Name
        </th>
        <th></th>
    </tr>
    @*  *@
@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Classroom.ClassroomName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FirstName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.LastName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
            @*@Html.ActionLink("Details", "Details", new { id=item.ID }) |*@
            @Html.ActionLink("Delete", "Delete", new { id=item.ID })
        </td>
    </tr>
}

</table>
