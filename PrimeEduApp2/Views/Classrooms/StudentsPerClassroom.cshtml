@model PrimeEduApp2.Models.Classroom

@{
    ViewBag.Title = "StudentsPerClassroom";
}

<h2>StudentsPerClassroom</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @*@Html.DisplayNameFor(model => model.ClassroomName)*@
            List Of Students
        </th>
        <th></th>
    </tr>

@foreach (var item in Model.Students) {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.FirstName)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.LastName)
    </td>
    <td>
        <a href="@Url.Action("Edit","Students", new { controller = "Students", id = item.ID})">Edit</a> |
        @*@Html.ActionLink("Details", "Details", "Students", new { id = item.ID }) |*@
        <a href="@Url.Action("Delete","Students", new { controller = "Students", id = item.ID})">Delete</a>
    </td>
</tr>
}

</table>
